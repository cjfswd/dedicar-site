// import vueScrollTo from 'vue-scrollto'

// export default function (context) {
//     if (context.route.fullPath == '/' || context.route.fullPath == '') {
//         context.redirect('/home')
//     }

//     // context.app.router.afterEach((to, from) => {
//     //     if (to.hash) {
//     //         vueScrollTo(context.route.to.hash, { force: true, cancelable: false })
//     //     } else {
//     //         vueScrollTo('body', { force: true, cancelable: false })
//     //     }
//     // })
// }
